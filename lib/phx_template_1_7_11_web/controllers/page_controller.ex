defmodule PhxTemplate1711Web.PageController do
  use PhxTemplate1711Web, :controller

  def home(conn, _params) do
    # The home page is often custom made,
    # so skip the default app layout.
    render(conn, :home, layout: false)
  end
end
